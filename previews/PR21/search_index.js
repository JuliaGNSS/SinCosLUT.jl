var documenterSearchIndex = {"docs":
[{"category":"section","location":"benchmarks/#Benchmarks","page":"Benchmarks","text":"These numbers compare SinCosLUT against the two polynomial JuliaGNSS packages — the float minimax polynomial FastSinCos.jl and the integer minimax polynomial FixedPointSinCosApproximations.jl. ps/elem is picoseconds per output element (computing both sin and cos); max abs error is the worst-case absolute error vs the true values over several cycles; ~bits is -log2(max error).\n\nnote: Why these tables are not run during the docs build\nUnlike the code examples elsewhere in this manual (which run on every build), the numbers below are measurements and depend on the host CPU — in particular on the presence of AVX-512 vpermb, which the shared CI runners that build these docs do not have. Running the suite there would produce slow, misleading figures. They are instead measured on fixed hardware and refreshed with each release. Reproduce them on your own machine with julia benchmark/comparison.jl.\n\nMeasured on an AMD Ryzen AI 9 HX PRO 370 (Zen 5) with Julia 1.12. The AVX2 blocks force the AVX2-width path on the same hardware.","title":"Benchmarks"},{"category":"section","location":"benchmarks/#Kernel-throughput","page":"Benchmarks","text":"Sin and cos for an array of pre-computed phase indices — the bare lookup/evaluate cost, with no phase generation.","title":"Kernel throughput"},{"category":"section","location":"benchmarks/#AVX-512","page":"Benchmarks","text":"method ps/elem max abs error ~bits\nSinCosLUT Int8, steps=64 11.7 5.2e-2 ~4\nSinCosLUT Int8, steps=128 13.9 2.8e-2 ~5\nFixedPoint Int16 (Val 7) 105 1.3e-2 ~6\nFastSinCos u100k 190 3.2e-4 ~12\nFixedPoint Int32 (Val 8) 209 8.1e-3 ~7\nFastSinCos u35 229 6.0e-8 ~24\nFixedPoint Int32 (Val 14) 297 1.1e-4 ~13","title":"AVX-512"},{"category":"section","location":"benchmarks/#AVX2","page":"Benchmarks","text":"No native cross-lane byte permute (vpermb is unavailable); the lookup is a half-table vpshufb+blend split plus a psignb sign flip (exploiting the table's half-wave anti-symmetry).\n\nmethod ps/elem max abs error ~bits\nSinCosLUT Int8, steps=64 21.2 5.2e-2 ~4\nFixedPoint Int16 (Val 7) 123 1.3e-2 ~6\nFixedPoint Int32 (Val 8) 250 8.1e-3 ~7\nFastSinCos u100k 250 3.2e-4 ~12\nFastSinCos u35 327 6.0e-8 ~24","title":"AVX2"},{"category":"section","location":"benchmarks/#End-to-end-carrier","page":"Benchmarks","text":"Phase generation plus sincos, at 0.01 cycles/sample — each package generates the carrier itself. This is where FixedPoint's multiplicative-inverse phase work shows; the kernel rows above are unaffected by it. SinCosLUT generates the phase with a UInt32 NCO accumulator (one add per step), so its carrier stays close to its bare kernel (≈17 vs ≈10 ps on AVX-512). All three are exact in phase.","title":"End-to-end carrier"},{"category":"section","location":"benchmarks/#AVX-512-2","page":"Benchmarks","text":"method ps/elem max abs error ~bits\nSinCosLUT Int8, steps=64 16.8 9.3e-2 ~3\nSinCosLUT Int8, steps=128 17.7 4.3e-2 ~5\nFixedPoint Int16 (Val 7) 131 1.5e-2 ~6\nFastSinCos u100k (Float32 phase) 237 3.1e-4 ~12\nFixedPoint Int32 (Val 13) 317 2.4e-4 ~12","title":"AVX-512"},{"category":"section","location":"benchmarks/#AVX2-2","page":"Benchmarks","text":"method ps/elem max abs error ~bits\nSinCosLUT Int8, steps=64 47.8 9.3e-2 ~3\nFixedPoint Int16 (Val 7) 144 1.5e-2 ~6\nFastSinCos u100k (Float32 phase) 328 3.1e-4 ~12\nFixedPoint Int32 (Val 13) 387 2.4e-4 ~12","title":"AVX2"},{"category":"section","location":"benchmarks/#Takeaways","page":"Benchmarks","text":"SinCosLUT is fastest on both AVX-512 and AVX2 — one vpermb per result on AVX-512, a half-table vpshufb+blend split with a psignb sign flip on AVX2 — but only ~4–5 bits accurate, set by the table's phase resolution.\nAVX2 is ~2–3× slower than AVX-512 here (half the lanes, plus the emulation of the missing byte permute), yet still the fastest option at its accuracy by a wide margin — the half-table split (v3.2) roughly doubled its kernel throughput over the earlier four-way split.\nOn AVX-512 the 128-entry table (two-register vpermi2b) doubles phase resolution to ~5 bits for a ~20% kernel premium — and no carrier premium, where the NCO hides it — so it is the better default there when the extra phase bit matters; the AVX2 split supports only the 64-entry table.\nFixedPoint's drift-free DDA carrier is only marginally slower than its bare kernel (131 vs 105 ps on AVX-512) — integer phase generation is nearly free. At ~6-bit accuracy FixedPoint Int16 is the fastest polynomial carrier; at float-grade accuracy FastSinCos edges FixedPoint Int32.\nNEON (not in the x86 tables above): on the CI Apple M1 runner the Int8 carrier runs at ~70 ps/elem, and the bytewise-tbl Int16 backend (v3.2) is ~2.2× faster than the scalar fallback it replaces (~220 vs ~510 ps/elem).\nPick by accuracy: ≤5-bit → SinCosLUT (AVX-512/AVX2/NEON); ~6–13-bit integer → FixedPointSinCosApproximations; float-grade (12–24-bit) → FastSinCos. See Choosing a package.","title":"Takeaways"},{"category":"section","location":"benchmarks/#Continuous-benchmarking","page":"Benchmarks","text":"A separate AirspeedVelocity.jl suite in benchmark/benchmarks.jl tracks throughput regressions across revisions (benchpkg SinCosLUT --rev=dirty). It covers generate_carrier! for each element type and buffer size, the 1-bit generate_carrier_signs! path, the 4-way interleaved fill, the fused single-stream reduction, and lookup_sincos!.","title":"Continuous benchmarking"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Tables","page":"API reference","text":"","title":"Tables"},{"category":"section","location":"api/#Carrier-generation","page":"API reference","text":"","title":"Carrier generation"},{"category":"section","location":"api/#Arbitrary-index-lookup","page":"API reference","text":"","title":"Arbitrary-index lookup"},{"category":"section","location":"api/#Value-based-carrier-engine","page":"API reference","text":"Allocation-free, register-resident NCO carrier for fusing into a correlation loop: a loop-invariant CarrierEngine plus an isbits CarrierState renewed by value each iteration. One engine/state pair serves any interleave factor K — hold K states carrier_state(eng, (k-1)*W) and advance each by K chunks per iteration. See Fused, array-free generation for the full pattern.","title":"Value-based carrier engine"},{"category":"section","location":"api/#Backends","page":"API reference","text":"","title":"Backends"},{"category":"module","location":"api/#SinCosLUT.SinCosLUT","page":"API reference","text":"SinCosLUT\n\nFast SIMD sine/cosine via register-resident byte/word/dword table lookups.\n\nThe lookup is a single hardware permute over a table held in vector registers: vpermb/vpermw/vpermd (AVX-512), vpshufb (AVX2, Int8 only), or NEON tbl (AArch64, Int8 and Int16). Output element type is selectable — Int8 (fastest, ~3-7 bit), Int16, or Int32 (more amplitude precision, fewer lanes/entries).\n\nAccuracy is a deliberate trade for speed: a 512-bit register holds 64×Int8 but only 32×Int16 / 16×Int32, so wider elements give finer amplitude but coarser phase resolution (fewer table entries) and lower throughput.\n\nSVE2 is not supported: Julia cannot express LLVM scalable vectors (<vscale x N x T>), see JuliaLang/julia#40308. NEON tbl runs natively on SVE hardware regardless.\n\n\n\n\n\n","title":"SinCosLUT.SinCosLUT"},{"category":"type","location":"api/#SinCosLUT.SinCosTable","page":"API reference","text":"SinCosTable(T=Int8; steps=64, amplitude=typemax(T))\n\nBuild sin/cos lookup tables of steps entries per full cycle, output type T (Int8/Int16/Int32). steps should be a power of two. For the AVX-512 backend steps must be regsize(T) (single permute) or 2*regsize(T) (two-register vpermi2): Int8 → 64 or 128, Int16 → 32 or 64, Int32 → 16 or 32. Other sizes still work on the portable backend.\n\n\n\n\n\n","title":"SinCosLUT.SinCosTable"},{"category":"function","location":"api/#SinCosLUT.generate_carrier!","page":"API reference","text":"generate_carrier!(sin_out, cos_out, table, freq_word::Integer;       phase=0, backend=…)\ngenerate_carrier!(sin_out, cos_out, table, cycles_per_sample::Real;  phase=0, backend=…)\ngenerate_carrier!(sin_out, cos_out, table; frequency, sampling_frequency, phase=0, backend=…)\n\nFill sin_out/cos_out (element type T) with a carrier generated by a textbook NCO (numerically-controlled oscillator): a UInt32 phase accumulator is advanced by a fixed frequency word each sample, and the top log2(steps) bits index the table. One full UInt32 is one carrier cycle, so the frequency resolution is uniform and dead-zone-free — sampling_frequency / 2^32 cycles per sample — and the phase never drifts (it is the exact closed form acc[n] = freq_word·n + offset mod 2^32, computed per lane with no division).\n\nThe low-level form takes a raw freq_word (a UInt32, 0 ≤ freq_word ≤ 2^32-1); a cycles_per_sample::Real is converted as freq_word = round(cps·2^32) (wraps via two's complement for negative/receding Doppler); the keyword form takes a frequency and sampling_frequency directly (cycles_per_sample = frequency / sampling_frequency). phase is the initial carrier phase, default 0: an Integer is table steps (exact), a Real is cycles.\n\n\n\n\n\n","title":"SinCosLUT.generate_carrier!"},{"category":"function","location":"api/#SinCosLUT.generate_carrier_signs!","page":"API reference","text":"generate_carrier_signs!(sin_signs, cos_signs, n, freq_word::Integer;      phase=0)\ngenerate_carrier_signs!(sin_signs, cos_signs, n, cycles_per_sample::Real; phase=0)\ngenerate_carrier_signs!(sin_signs, cos_signs, n; frequency, sampling_frequency, phase=0)\n\nGenerate a 1-bit (hard-limited) carrier: pack the sign bits of the NCO sin/cos for n samples into sin_signs/cos_signs (UInt64, each ≥ cld(n, 64) words). Bit j of word w corresponds to sample 64w + j (0-based); a set bit means that component is negative (i.e. its ±1 hard-limited value is −1). Bits past sample n in the last word are cleared.\n\nThis is the same NCO as generate_carrier! — a UInt32 phase accumulator advanced by a fixed frequency word each sample (acc[n] = freq_word·n + offset mod 2³²) — but only the sign is kept, read directly off the accumulator's top bit (sign(sin) = MSB(acc), sign(cos) = MSB(acc + ¼ cycle)), so there is no table and no output-type quantisation. The frequency argument matches generate_carrier!: a raw UInt32 freq_word, a cycles_per_sample::Real (freq_word = round(cps·2³²)), or the frequency/sampling_frequency keyword form. phase is the initial carrier phase in cycles (a Real; default 0).\n\nFast at any frequency: a 1-bit carrier is a square wave, so for a low residual carrier almost every 64-sample word is a single constant run written with one store; where a sign flip falls inside a word, the 64 signs are packed with a single SIMD sign-mask (no per-sample loop). This is the carrier form a bit-wise correlator consumes, where wipe-off becomes XOR and accumulation becomes popcount.\n\nn = 5000\nsin_signs = Vector{UInt64}(undef, cld(n, 64))\ncos_signs = Vector{UInt64}(undef, cld(n, 64))\ngenerate_carrier_signs!(sin_signs, cos_signs, n; frequency = 1234, sampling_frequency = 5e6)\n\n\n\n\n\n","title":"SinCosLUT.generate_carrier_signs!"},{"category":"function","location":"api/#SinCosLUT.cycles_per_sample","page":"API reference","text":"cycles_per_sample(frequency, sampling_frequency)\n\nNormalised frequency frequency / sampling_frequency (cycles per sample) — pass the result as the frequency argument of generate_carrier! or carrier_engine, e.g. generate_carrier!(sin_out, cos_out, table, cycles_per_sample(1000, 2e6)).\n\n\n\n\n\n","title":"SinCosLUT.cycles_per_sample"},{"category":"function","location":"api/#SinCosLUT.lookup_sincos!","page":"API reference","text":"lookup_sincos!(sin_out, cos_out, phase_indices, table; backend=default_backend(table))\n\nLook up sin/cos for each integer phase index in phase_indices (taken mod steps).\n\n\n\n\n\n","title":"SinCosLUT.lookup_sincos!"},{"category":"function","location":"api/#SinCosLUT.prepare","page":"API reference","text":"prepare(table; backend=default_backend(table)) -> callable\n\nMaterialise table into registers once and return a callable p such that p(phase_index::Vec) -> (sin::Vec, cos::Vec) (indices taken mod steps). Analogous to FastSinCos's fast_sincos_*, but a table lookup.\n\nThe callable's concrete type can depend on the table's VALUES (the AVX2 backend picks a smaller register layout for the half-wave-symmetric tables the constructor builds), so build it once outside hot loops.\n\n\n\n\n\n","title":"SinCosLUT.prepare"},{"category":"type","location":"api/#SinCosLUT.CarrierEngine","page":"API reference","text":"CarrierEngine\n\nImmutable, loop-invariant NCO carrier engine built once by carrier_engine: holds the register-resident sin/cos table and the phase-accumulator frequency word. Pair it with one CarrierState per interleaved stream and drive it with carrier_lookup / carrier_advance. It is isbits, so it costs no allocation.\n\n\n\n\n\n","title":"SinCosLUT.CarrierEngine"},{"category":"type","location":"api/#SinCosLUT.CarrierState","page":"API reference","text":"CarrierState{W}\n\nImmutable, isbits NCO phase-accumulator state (a Vec{W,UInt32}). Created by carrier_state and renewed by value each iteration via carrier_advance; read with carrier_lookup. Holds no table data, so it stays in registers.\n\n\n\n\n\n","title":"SinCosLUT.CarrierState"},{"category":"function","location":"api/#SinCosLUT.carrier_engine","page":"API reference","text":"carrier_engine(table, freq_word::Integer;      backend=…) -> CarrierEngine\ncarrier_engine(table, cycles_per_sample::Real; backend=…) -> CarrierEngine\ncarrier_engine(table; frequency, sampling_frequency, backend=…) -> CarrierEngine\n\nBuild the loop-invariant carrier engine for table. The NCO phase advances by a UInt32 frequency word per sample; the low-level form takes it raw (0 ≤ freq_word ≤ 2^32-1), cycles_per_sample::Real converts as round(cps·2^32), and the keyword form derives it from frequency/sampling_frequency. The phase is uniform, dead-zone-free and never drifts.\n\n\n\n\n\n","title":"SinCosLUT.carrier_engine"},{"category":"function","location":"api/#SinCosLUT.carrier_state","page":"API reference","text":"carrier_state(eng::CarrierEngine, start::Integer = 0; phase = 0) -> CarrierState\n\nInitial NCO state for a stream whose first sample is absolute index start (use (k-1)·W for the k-th lane of a W-wide, K-way interleaved loop). phase is the initial carrier phase added to every lane: an Integer is table steps, a Real is cycles.\n\n\n\n\n\n","title":"SinCosLUT.carrier_state"},{"category":"function","location":"api/#SinCosLUT.carrier_lookup","page":"API reference","text":"carrier_lookup(eng::CarrierEngine, st::CarrierState) -> (sin::Vec{W,T}, cos::Vec{W,T})\n\nThe (sin, cos) chunk at st's current phase. Pure read — does not advance the state.\n\n\n\n\n\n","title":"SinCosLUT.carrier_lookup"},{"category":"function","location":"api/#SinCosLUT.carrier_advance","page":"API reference","text":"carrier_advance(eng::CarrierEngine, st::CarrierState, nchunks::Integer) -> CarrierState\n\nAdvance the stream by nchunks W-wide chunks (nchunks·W samples), returning a new immutable state. A K-way interleaved loop advances every state by K each iteration.\n\n\n\n\n\n","title":"SinCosLUT.carrier_advance"},{"category":"function","location":"api/#SinCosLUT.carrier_width","page":"API reference","text":"carrier_width(eng::CarrierEngine) -> Int\n\nSIMD lane count W of the engine (samples per chunk).\n\n\n\n\n\n","title":"SinCosLUT.carrier_width"},{"category":"function","location":"api/#SinCosLUT.default_backend","page":"API reference","text":"default_backend(table::SinCosTable) -> Backend\ndefault_backend(T::Type, steps::Integer) -> Backend\n\nThe lookup backend automatically selected for output type T and table size steps on the host CPU: AVX512(), AVX2(), or Neon() where the required SIMD permute is available, otherwise Portable() (the always-correct scalar fallback). AVX2 supports only Int8 with steps = 64; NEON supports Int8 (steps = 64) and Int16 (steps = 32 or 64); other combinations fall back to Portable().\n\nThe x86 ISA backends (AVX-512/AVX2) are selected only when the LLVM codegen target is native. Under a restricted or multiversioned CPU target (--cpu-target=… / JULIA_CPU_TARGET=…, the latter being how the official binaries build every pkgimage) the CPU-detected features need not match what LLVM can emit, so those backends fall back to Portable() rather than bake un-legalisable ISA into a baseline clone (which would abort codegen). Set JULIA_CPU_TARGET=native to keep the ISA backends under a custom target.\n\nPass the result (or any Backend) as the backend keyword of generate_carrier!, lookup_sincos!, carrier_engine, or prepare to override the choice. See backend_name for a readable label.\n\n\n\n\n\n","title":"SinCosLUT.default_backend"},{"category":"function","location":"api/#SinCosLUT.backend_name","page":"API reference","text":"backend_name(backend) -> String\n\nHuman-readable name of a Backend (\"AVX-512\", \"AVX2\", \"NEON\", or \"portable\"). Useful for reporting which lookup primitive default_backend selected.\n\n\n\n\n\n","title":"SinCosLUT.backend_name"},{"category":"section","location":"guide/#Usage-guide","page":"Usage guide","text":"Every code block below is run when the docs are built, so the printed outputs are the real values produced by the current version.","title":"Usage guide"},{"category":"section","location":"guide/#Generating-a-carrier","page":"Usage guide","text":"generate_carrier! fills a pair of pre-allocated sin/cos buffers with a carrier at a given normalised frequency (cycles per sample). The output element type is the table's element type.\n\nusing SinCosLUT\n\ntbl  = SinCosTable(Int8; steps = 64)   # 64-entry Int8 table\nsins = zeros(Int8, 16)\ncoss = zeros(Int8, 16)\n\ngenerate_carrier!(sins, coss, tbl, 0.05)   # 0.05 cycles/sample\nsins","title":"Generating a carrier"},{"category":"section","location":"guide/#Specifying-the-frequency","page":"Usage guide","text":"The frequency can be given three equivalent ways. Use whichever matches the numbers you already have:\n\nsins = zeros(Int8, 16); coss = zeros(Int8, 16)\n\n# 1. Directly as cycles/sample (= frequency / sampling_frequency):\ngenerate_carrier!(sins, coss, tbl, 0.0005)\n\n# 2. From a frequency and sampling frequency (keyword form):\ngenerate_carrier!(sins, coss, tbl; frequency = 1000, sampling_frequency = 2e6)  # 1 kHz at 2 MHz\n\n# 3. Via the cycles_per_sample helper:\ngenerate_carrier!(sins, coss, tbl, cycles_per_sample(1000, 2e6))                # → 0.0005\nnothing # hide\n\nAny frequency resolves on a uniform sampling_frequency / 2^32 grid (≈ 0.001 Hz at 5 MHz) — arbitrary Doppler works with no dead-zone near DC and never errors:\n\ngenerate_carrier!(sins, coss, tbl; frequency = 1234.567, sampling_frequency = 5e6)\nnothing # hide\n\nSee Accuracy & drift-free phase for what that grid means for precision.","title":"Specifying the frequency"},{"category":"section","location":"guide/#Initial-phase","page":"Usage guide","text":"The phase keyword sets the starting carrier phase (default 0). An Integer is counted in table steps; a Real is counted in cycles — so on a 64-step table, 16 steps and 0.25 cycles are the same quarter cycle:\n\ns1 = zeros(Int8, 8); c1 = zeros(Int8, 8)\ns2 = zeros(Int8, 8); c2 = zeros(Int8, 8)\ngenerate_carrier!(s1, c1, tbl, 0.05; phase = 16)     # 16 table steps  (¼ cycle)\ngenerate_carrier!(s2, c2, tbl, 0.05; phase = 0.25)   # 0.25 cycles = 90°, same result\n(s1 == s2, c1 == c2)","title":"Initial phase"},{"category":"section","location":"guide/#Higher-amplitude-precision","page":"Usage guide","text":"Switch the table's element type to Int16 or Int32 for finer amplitude quantisation (at the cost of throughput and phase resolution — see The speed/precision trade):\n\ntbl16 = SinCosTable(Int16; steps = 64)   # 6-bit phase, 15-bit amplitude (vpermi2w on AVX-512)\ns16 = zeros(Int16, 8); c16 = zeros(Int16, 8)\ngenerate_carrier!(s16, c16, tbl16, 0.05)\ns16","title":"Higher amplitude precision"},{"category":"section","location":"guide/#Looking-up-arbitrary-phase-indices","page":"Usage guide","text":"If you already have integer phase indices (not a swept carrier), lookup_sincos! maps each index (taken mod steps) to its table entry:\n\nphases = Int8[0, 8, 16, 24, 32, 40, 48, 56]   # eighths of a cycle on a 64-step table\ns = zeros(Int8, 8); c = zeros(Int8, 8)\nlookup_sincos!(s, c, phases, tbl)\ns","title":"Looking up arbitrary phase indices"},{"category":"section","location":"guide/#Choosing-a-backend","page":"Usage guide","text":"The lookup primitive is picked automatically from the host CPU:\n\nbackend instruction types notes\nAVX-512 vpermb/vpermw/vpermd (+vpermi2*) Int8/16/32 fastest\nAVX2 vpshufb + blends Int8 only 64-entry table, half-table split + sign flip\nNEON tbl (tbl4) Int8, Int16 AArch64; Int16 is looked up bytewise\nportable scalar Int8/16/32 always available\n\nAVX2 and NEON have only a byte table permute (vpshufb / tbl) — the word/dword permutes (vpermw/vpermd) are AVX-512. On AVX2 the SIMD lookup therefore exists for Int8 (steps = 64) only. On NEON, Int16 (steps = 32 or 64) is additionally served by looking the table up bytewise (each word index becomes its little-endian byte pair). Int32 — and Int16 on AVX2 — fall back to the (correct, but scalar) portable backend; at those accuracies, prefer a polynomial package over a wider SinCosLUT element type on such hosts.\n\nYou can query the choice, or force one (e.g. for testing, or to skip the runtime CPU check):\n\nusing SinCosLUT: AVX512, AVX2, Portable\n\nbackend_name(default_backend(Int8, 64))   # what will run here\n\nnote: Restricted or multiversioned CPU targets\nThe x86 ISA backends are chosen only when the LLVM codegen target is native (the default). Under a restricted or multiversioned CPU target — julia --cpu-target=…, or JULIA_CPU_TARGET=…, which is how the official binaries build every pkgimage — the CPU-detected features need not match what LLVM can actually emit, so default_backend falls back to Portable() for correctness (emitting an ISA the target cannot legalise would abort codegen with an uncatchable LLVM ERROR). Set JULIA_CPU_TARGET=native to keep the AVX-512/AVX2 backends under a custom target. When this fallback demotes a SIMD-capable CPU, SinCosLUT emits a one-time load-time warning with this hint; silence it with ENV[\"SINCOSLUT_QUIET\"] = \"1\" (e.g. in a deliberately restricted or compiled-app build).\n\nThe results are backend-independent — forcing the scalar path produces exactly the same values, just computed without SIMD:\n\ns_auto = zeros(Int8, 32); c_auto = zeros(Int8, 32)\ns_port = zeros(Int8, 32); c_port = zeros(Int8, 32)\ngenerate_carrier!(s_auto, c_auto, tbl, 0.05)\ngenerate_carrier!(s_port, c_port, tbl, 0.05; backend = Portable())\n(s_auto == s_port, c_auto == c_port)","title":"Choosing a backend"},{"category":"section","location":"guide/#One-bit-(hard-limited)-carrier","page":"Usage guide","text":"For bit-wise (\"bit-sliced\") software correlators the carrier is quantised to a single sign bit, so wipe-off becomes XOR and accumulation becomes popcount. generate_carrier_signs! produces that directly — no table, no output-type quantisation — by reading the sign straight off the same UInt32 NCO (sign(sin) = MSB(acc), sign(cos) = MSB(acc + ¼ cycle)) and packing it into UInt64 words. Bit j of word w corresponds to sample 64w + j; a set bit means that component is negative.\n\nn = 200\nsin_signs = Vector{UInt64}(undef, cld(n, 64))\ncos_signs = Vector{UInt64}(undef, cld(n, 64))\n# same frequency forms as generate_carrier!\ngenerate_carrier_signs!(sin_signs, cos_signs, n; frequency = 1234, sampling_frequency = 5e6)\n\n# bit 0 of word 1 is the sign of sample 0:\nfirst_sin_negative = (sin_signs[1] & 1) != 0\n\nA 1-bit carrier is a square wave, so almost every 64-sample word is a single constant run written with one store; only words straddling a sign flip are filled with a single SIMD sign-mask. It therefore stays fast at any frequency.","title":"One-bit (hard-limited) carrier"},{"category":"section","location":"fused/#Fused,-array-free-generation","page":"Fused, array-free","text":"generate_carrier! is the right tool when you actually want a carrier array. But if you are going to consume the carrier immediately — correlating it against a signal, say — materialising it first wastes a store and the cache traffic that comes with it. The value-based engine lets you pull (sin, cos) chunks straight into registers and never touch memory.\n\nBuild a loop-invariant CarrierEngine once with carrier_engine, then drive it with an isbits CarrierState that you renew by value each iteration. The drift-free NCO lives in the state, so the loop allocates nothing and nothing escapes to the heap. It returns SIMD.Vecs, the same shape as FastSinCos.","title":"Fused, array-free generation"},{"category":"section","location":"fused/#A-fused-correlation-loop","page":"Fused, array-free","text":"using SinCosLUT, SIMD\n\n# correlate a carrier against a signal without ever building the carrier array\nfunction correlate(tbl, cps, signal)\n    eng = carrier_engine(tbl, cps)\n    W = carrier_width(eng); st = carrier_state(eng); acc = 0; i = 1\n    @inbounds for _ in 1:(length(signal) ÷ W)\n        s, c = carrier_lookup(eng, st)             # s, c :: Vec{W,Int8}\n        sg = signal[VecRange{W}(i)]\n        acc += sum(Vec{W,Int32}(s) * Vec{W,Int32}(sg))\n        i += W; st = carrier_advance(eng, st, 1)\n    end\n    acc\nend\n\ntbl    = SinCosTable(Int8; steps = 64)\nsignal = rand(Int8, 4096)\ncorrelate(tbl, 0.002, signal)\n\ncarrier_engine(table, cycles_per_sample) (or ; frequency, sampling_frequency) builds the engine.\ncarrier_lookup returns the (sin, cos)::Tuple{Vec{W,T},Vec{W,T}} chunk at the current phase — W is the backend's SIMD width. It is a pure read.\ncarrier_advance(eng, st, nchunks) steps the state forward by nchunks W-wide chunks, returning a new state.\ncarrier_width is W.\n\nThe loop above allocates nothing beyond its inputs:\n\ncorrelate(tbl, 0.002, signal)      # warm up / compile\n@allocated correlate(tbl, 0.002, signal)","title":"A fused correlation loop"},{"category":"section","location":"fused/#Interleaving-for-full-throughput","page":"Fused, array-free","text":"A single stream is latency-bound on its one NCO carry chain. When the per-sample work is light (e.g. just filling an array), interleave K independent streams so the carry chains overlap and the loop reaches its full rate. Hold K states positioned at carrier_state(eng, (k-1)*W) and advance each by K chunks per iteration:\n\nfunction fill4!(sins, coss, tbl, cps)\n    eng = carrier_engine(tbl, cps); W = carrier_width(eng)\n    st0 = carrier_state(eng, 0);  st1 = carrier_state(eng, W)\n    st2 = carrier_state(eng, 2W); st3 = carrier_state(eng, 3W)\n    i = 1\n    @inbounds for _ in 1:(length(sins) ÷ (4W))\n        s0,c0 = carrier_lookup(eng, st0); s1,c1 = carrier_lookup(eng, st1)\n        s2,c2 = carrier_lookup(eng, st2); s3,c3 = carrier_lookup(eng, st3)\n        sins[VecRange{W}(i)]    = s0; coss[VecRange{W}(i)]    = c0\n        sins[VecRange{W}(i+W)]  = s1; coss[VecRange{W}(i+W)]  = c1\n        sins[VecRange{W}(i+2W)] = s2; coss[VecRange{W}(i+2W)] = c2\n        sins[VecRange{W}(i+3W)] = s3; coss[VecRange{W}(i+3W)] = c3\n        i += 4W\n        st0 = carrier_advance(eng, st0, 4); st1 = carrier_advance(eng, st1, 4)\n        st2 = carrier_advance(eng, st2, 4); st3 = carrier_advance(eng, st3, 4)\n    end\n    sins, coss\nend\n\nW = carrier_width(carrier_engine(tbl, 0.002))\nsins = zeros(Int8, 8W); coss = zeros(Int8, 8W)\nfill4!(sins, coss, tbl, 0.002)\nsins[1:8]\n\nAt scale this matches generate_carrier! (which interleaves internally — 4 streams where registers allow, fewer where they don't: AVX2's 16 registers favour a single stream, NEON's Int16 tables leave room for 2).\n\ntip: How many streams?\nUse a single stream when you fuse into nontrivial work — it borrows the ILP of your own loop. Use a 4-way interleave (or just generate_carrier!) when the per-sample work is light — except on AVX2, where 4×4 accumulator registers plus the table exceed the 16 YMM registers and the spills cost more than the interleave buys.","title":"Interleaving for full throughput"},{"category":"section","location":"fused/#The-stateless-primitive","page":"Fused, array-free","text":"For the FastSinCos-style primitive where you supply the phase indices, prepare builds the register-resident table once and returns a callable. It maps a Vec of phase indices to (sin, cos) Vecs, so the input must be the backend's SIMD width W (as it would be inside a VecRange loop):\n\np = prepare(tbl)                                   # build table in registers once\nW = carrier_width(carrier_engine(tbl, 0.0))        # the backend's SIMD width\nidx  = Vec{W,Int8}(ntuple(j -> Int8((j - 1) & 63), W))\ns, c = p(idx)                                      # idx -> (sin, cos), like fast_sincos_*(::Vec)\n(s[1], c[1])                                       # phase index 0 → (sin, cos) = (0, 127)","title":"The stateless primitive"},{"category":"section","location":"accuracy/#Accuracy-and-drift-free-phase","page":"Accuracy & drift-free phase","text":"SinCosLUT quantises in two independent dimensions. Understanding them tells you exactly what to expect from the output.","title":"Accuracy & drift-free phase"},{"category":"section","location":"accuracy/#What-is-quantised","page":"Accuracy & drift-free phase","text":"Phase is quantised to steps entries per cycle. A 64-entry table gives ~6-bit phase resolution; the larger 2·regsize(T) tables (via vpermi2 on AVX-512) double it — Int8 → 128 entries, ~7-bit phase.\nAmplitude is quantised to the output type's range (Int8 ≈ 7-bit, Int16 ≈ 15-bit, Int32 ≈ 31-bit).\nNo range limit: any phase — positive, negative, or huge — is reduced exactly by the index bit-mask. There is no Cody–Waite-style ceiling and no precision loss at large arguments, unlike float approximations.\n\nWe can see the amplitude quantisation directly by comparing the normalised table output to the true sin/cos:\n\nusing SinCosLUT\n\ntbl   = SinCosTable(Int8; steps = 128)   # finest Int8 phase resolution\nn     = 4096\nsins  = zeros(Int8, n); coss = zeros(Int8, n)\ncps   = 0.01\ngenerate_carrier!(sins, coss, tbl, cps)\n\namp = Float64(typemax(Int8))\nmaxerr = maximum(1:n) do i\n    φ = 2π * cps * (i - 1)\n    max(abs(sins[i]/amp - sin(φ)), abs(coss[i]/amp - cos(φ)))\nend\n(maxerr, \"≈ $(round(-log2(maxerr), digits=1)) bits\")\n\nFor anything needing more than a few bits, a polynomial approximation is the better tool — see Choosing a package.","title":"What is quantised"},{"category":"section","location":"accuracy/#Drift-free-phase","page":"Accuracy & drift-free phase","text":"generate_carrier! is a UInt32 NCO (numerically-controlled oscillator). The phase index for sample n is\n\nleftlfloor fractextttfreq_word cdot n2^32 - log_2 textttsteps rightrfloor\nqquad textttfreq_word = operatornameroundleft(fracff_s cdot 2^32right)\n\nevaluated in exact integer arithmetic — the accumulator is freq_word·n mod 2^32. The phase therefore accumulates no rounding error, even over billions of samples, unlike a floating-point acc += step accumulator that drifts a little more with every add.\n\nThe only residual is that the synthesised frequency is quantised to the nearest f_s / 2^32 (≈ 0.0006 Hz at 5 MHz) — a uniform, dead-zone-free grid. That is a fixed sub-milliHz frequency offset, not an accumulating drift.\n\nThe point is not that the accumulator hits some magic value, but that accumulating it incrementally (one add per sample) gives the same answer as the exact closed form, no matter how many samples you run. We can show that directly: replay acc += step for n samples and compare to the closed form step · n, once with SinCosLUT's UInt32 frequency word and once with a naive Float32 step.\n\ncps       = 1234.5678 / 5_000_000              # cycles/sample, not nicely representable\nfreq_word = UInt32(round(Int64, cps * 4.294967296e9))\nstep_f32  = Float32(cps)\n\nint_incremental(n) = (acc = UInt32(0); for _ in 1:n; acc += freq_word; end; acc)\nf32_incremental(n) = (acc = 0.0f0;     for _ in 1:n; acc += step_f32;  end; acc)\n\nint_closed(n) = freq_word * UInt32(n)          # exact, wraps mod 2^32\nf32_closed(n) = step_f32 * Float32(n)\n\n[(n = n,\n  integer_drift        = Int(int_incremental(n)) - Int(int_closed(n)),   # accumulator units\n  float32_drift_cycles = f32_incremental(n) - f32_closed(n))\n for n in (10^3, 10^5, 10^7)]\n\nThe integer NCO's integer_drift is exactly 0 at every horizon — the incremental sum is the closed form, because UInt32 addition is exact. The Float32 accumulator, by contrast, drifts further with every add: by n = 10^7 it is off by tens of cycles. That is the difference between a bounded frequency offset and an accumulating drift.","title":"Drift-free phase"},{"category":"section","location":"accuracy/#Choosing-a-package","page":"Accuracy & drift-free phase","text":"Three JuliaGNSS packages compute fast SIMD sin/cos at different points on the speed/accuracy curve:\n\naccuracy needed use\n≤ ~5-bit (very high throughput) SinCosLUT (this package)\n~6–13-bit, integer output FixedPointSinCosApproximations.jl\nfloat-grade, 12–24-bit FastSinCos.jl\n\nThe Benchmarks page quantifies the trade with measured throughput and error.","title":"Choosing a package"},{"category":"section","location":"#SinCosLUT.jl","page":"Home","text":"Fast SIMD sine/cosine by register-resident table lookup. Each sin/cos is a single hardware byte/word/dword permute over a table held entirely in vector registers — no multiplies, no memory gathers, and (unlike floating-point range reduction) no input range limit: the phase wraps for free via a bit-mask.\n\nIt is a deliberately low-precision, very high-throughput technique, built for GNSS carrier generation and bit-wise software correlators. If you need more than a few bits of accuracy, reach for a polynomial approximation instead (FixedPointSinCosApproximations.jl or FastSinCos.jl); see Choosing a package and Benchmarks.","title":"SinCosLUT.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"SinCosLUT\")","title":"Installation"},{"category":"section","location":"#The-speed/precision-trade","page":"Home","text":"Pick the output element type to trade precision for speed. A 512-bit register holds 64×Int8 but only 32×Int16 / 16×Int32, so wider elements give more amplitude bits but fewer table entries (coarser phase) and fewer lanes (lower throughput):\n\noutput T lanes (AVX-512) table entries / cycle ~accuracy\nInt8 64 64 or 128 ~3–7 bit\nInt16 32 32 or 64 finer amplitude\nInt32 16 16 or 32 finest amplitude","title":"The speed/precision trade"},{"category":"section","location":"#Quick-start","page":"Home","text":"The examples on this page (and throughout the docs) are executed when the manual is built, against the current source — if the API changes under them, the build fails, so they cannot go stale.\n\nusing SinCosLUT\n\n# 64-entry Int8 table (6-bit phase, full Int8 amplitude)\ntbl = SinCosTable(Int8; steps = 64)\n\nsins = zeros(Int8, 8)\ncoss = zeros(Int8, 8)\n\n# Carrier of 0.05 cycles/sample, drift-free integer phase:\ngenerate_carrier!(sins, coss, tbl, 0.05)\nsins\n\ncoss\n\nFrom here:\n\nUsage guide — carrier generation, frequency forms, initial phase, amplitude precision, backends, arbitrary-index lookup, and the 1-bit carrier.\nFused, array-free generation — the allocation-free carrier_engine for fusing sin/cos straight into a correlation loop.\nAccuracy & drift-free phase — what the quantisation buys you and why the phase never drifts.\nBenchmarks — throughput and accuracy against the polynomial packages.\nAPI reference — every exported function.\n\nnote: SVE2 is not supported\nJulia cannot express LLVM scalable vectors (<vscale x N x T>), see JuliaLang/julia#40308. NEON tbl runs natively on SVE hardware regardless.","title":"Quick start"}]
}
