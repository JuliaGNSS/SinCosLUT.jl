var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SinCosLUT.jl","page":"Home","text":"Fast SIMD sine/cosine by register-resident table lookup. Each sin/cos is a single hardware byte/word/dword permute over a table held entirely in vector registers (vpermb/vpermw/vpermd on AVX-512, vpshufb on AVX2, NEON tbl on AArch64) — no multiplies, no memory gathers, and no input range limit (the phase wraps for free via a bit-mask).\n\nIt is a deliberately low-precision, very high-throughput technique. Choose the output element type to trade precision for speed:\n\noutput T lanes (AVX-512) entries / cycle ~accuracy\nInt8 64 64 or 128 ~3–7 bit\nInt16 32 32 or 64 finer amplitude\nInt32 16 16 or 32 finest amplitude\n\nSee the README for backend details, the accuracy/range discussion, and the drift-free phase generation.\n\nnote: SVE2\nSVE2 is not supported: Julia cannot express LLVM scalable vectors (<vscale x N x T>), see JuliaLang/julia#40308. NEON tbl runs natively on SVE hardware regardless.","title":"SinCosLUT.jl"},{"category":"section","location":"#Quick-start","page":"Home","text":"using SinCosLUT\n\ntbl  = SinCosTable(Int8; steps = 64)\nsins = zeros(Int8, 4096); coss = zeros(Int8, 4096)\ngenerate_carrier!(sins, coss, tbl, 0.002)          # drift-free carrier, 0.002 cycles/sample\n\nArray-free / fused (returns Vecs, like FastSinCos):\n\nfor (s, c) in generate_carrier(tbl, 0.002, length(signal))   # s, c :: Vec{W,Int8}\n    # consume in registers; no carrier array is allocated\nend","title":"Quick start"},{"category":"section","location":"#API","page":"Home","text":"","title":"API"},{"category":"section","location":"#Tables","page":"Home","text":"","title":"Tables"},{"category":"section","location":"#Carrier-generation","page":"Home","text":"","title":"Carrier generation"},{"category":"section","location":"#Lookup-primitives","page":"Home","text":"","title":"Lookup primitives"},{"category":"type","location":"#SinCosLUT.SinCosTable","page":"Home","text":"SinCosTable(T=Int8; steps=64, amplitude=typemax(T))\n\nBuild sin/cos lookup tables of steps entries per full cycle, output type T (Int8/Int16/Int32). steps should be a power of two. For the AVX-512 backend steps must be regsize(T) (single permute) or 2*regsize(T) (two-register vpermi2): Int8 → 64 or 128, Int16 → 32 or 64, Int32 → 16 or 32. Other sizes still work on the portable backend.\n\n\n\n\n\n","title":"SinCosLUT.SinCosTable"},{"category":"function","location":"#SinCosLUT.generate_carrier!","page":"Home","text":"generate_carrier!(sin_out, cos_out, table, step_numerator, step_denominator; phase=0, backend=…)\ngenerate_carrier!(sin_out, cos_out, table, cycles_per_sample::Real;          phase=0, backend=…)\ngenerate_carrier!(sin_out, cos_out, table; frequency, sampling_frequency,    phase=0, backend=…)\n\nFill sin_out/cos_out (element type T) with a carrier whose phase advances by an exact step_numerator / step_denominator table-steps per sample (drift-free integer DDA). cycles_per_sample is the normalised frequency f/fs (the carrier completes that many cycles per sample, i.e. out[n] ≈ cos(2π·cycles_per_sample·n)); its step is cycles_per_sample * steps, rationalised internally. The third form takes a frequency and sampling_frequency directly (cycles_per_sample = frequency / sampling_frequency). phase is the initial carrier phase, default 0: an Integer is table steps (exact), a Real is cycles. Requires 0 < step_denominator ≤ typemax(T).\n\n\n\n\n\n","title":"SinCosLUT.generate_carrier!"},{"category":"function","location":"#SinCosLUT.generate_carrier","page":"Home","text":"generate_carrier(table, step_numerator, step_denominator, num_samples; phase=0, backend=…)\ngenerate_carrier(table, cycles_per_sample::Real,          num_samples; phase=0, backend=…)\ngenerate_carrier(table, num_samples; frequency, sampling_frequency,    phase=0, backend=…)\n\nIterator over num_samples ÷ W chunks (W = SIMD width for the backend/type), each yielding (sin, cos)::Tuple{Vec{W,T},Vec{W,T}}. Phase advances by an exact step_numerator / step_denominator (or cycles_per_sample·steps) table-steps per sample via a drift-free DDA held in the iteration state — no carrier array is allocated. phase is the initial carrier phase (default 0): an Integer is table steps, a Real is cycles. Requires 0 < step_denominator ≤ typemax(T). Any leftover num_samples % W tail is not produced (handle it yourself if needed).\n\n\n\n\n\n","title":"SinCosLUT.generate_carrier"},{"category":"function","location":"#SinCosLUT.generate_carrier4","page":"Home","text":"generate_carrier4(table, step_numerator, step_denominator, num_samples; phase=0, backend=…)\ngenerate_carrier4(table, cycles_per_sample, num_samples;                phase=0, backend=…)\ngenerate_carrier4(table, num_samples; frequency, sampling_frequency,    phase=0, backend=…)\n\nLike generate_carrier but yields a 4-tuple of (sin, cos) Vec pairs per step (4W samples), running four interleaved DDA states so the carry chains overlap. Reaches the full loop throughput (~40 ps/elem) even for trivial consumers such as array fill. Destructure the 4-tuple in the loop header — for ((s0,c0),(s1,c1),(s2,c2),(s3,c3)) in generate_carrier4(...) — rather than iterating it with an inner for pair in quad loop, which does not unroll and is much slower. phase is the initial carrier phase (default 0): Integer = table steps, Real = cycles. Produces num_samples ÷ (4W) steps; handle any tail yourself.\n\n\n\n\n\n","title":"SinCosLUT.generate_carrier4"},{"category":"function","location":"#SinCosLUT.cycles_per_sample","page":"Home","text":"cycles_per_sample(frequency, sampling_frequency)\n\nNormalised frequency frequency / sampling_frequency (cycles per sample) — pass the result as the frequency argument of generate_carrier!, generate_carrier or generate_carrier4, e.g. generate_carrier!(sin_out, cos_out, table, cycles_per_sample(1000, 2e6)).\n\n\n\n\n\n","title":"SinCosLUT.cycles_per_sample"},{"category":"function","location":"#SinCosLUT.lookup_sincos!","page":"Home","text":"lookup_sincos!(sin_out, cos_out, phase_indices, table; backend=default_backend(table))\n\nLook up sin/cos for each integer phase index in phase_indices (taken mod steps).\n\n\n\n\n\n","title":"SinCosLUT.lookup_sincos!"},{"category":"function","location":"#SinCosLUT.prepare","page":"Home","text":"prepare(table; backend=default_backend(table)) -> callable\n\nMaterialise table into registers once and return a callable p such that p(phase_index::Vec) -> (sin::Vec, cos::Vec) (indices taken mod steps). Analogous to FastSinCos's fast_sincos_*, but a table lookup.\n\n\n\n\n\n","title":"SinCosLUT.prepare"}]
}
